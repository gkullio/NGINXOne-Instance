# YourProject
You need the NGINX One License rename them to match
nginx-repo.crt
nginx-repo.key
nginx-repo.jwt

Change terraform.tfvars.boilerplate file and rename to terraform.tfvars

Get a new NGINX One Dataplane token and fill the terraform.tfvars file